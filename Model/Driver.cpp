#include "Driver.h"

Driver::Driver(const string &id, const string &name, const string &surname, const string &status) : id(id), name(name),
                                                                                                    surname(surname),
                                                                                                    status(status) {}

Driver::~Driver() {

}